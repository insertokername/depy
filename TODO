TODO:
  faster operations by not making bucket adding necessary
  ability to specify manifest just from .json
  make operations faster by packing them into a single env set
  delete shortcuts
  cleanup path operation

  pacakge search:
    show "no packages found" screen if no packages were found 
    better error handling AGAIN

  manage buckets button ( add remove update )
  